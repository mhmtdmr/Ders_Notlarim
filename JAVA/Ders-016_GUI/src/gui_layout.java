import java.awt.*;
import javax.swing.*;

public class gui_layout{

	public static void main(String[] args) {
		
		// GridBagLayout		
		
		/*
		// Ana çerçeveyi oluşturma
        JFrame frame = new JFrame("İlk Grafik Arayüzlü Programımız");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(500,400);
        
        
        
        // Menü ve bileşenlerini ekleme
        JMenuBar mb = new JMenuBar();
        JMenu m1 = new JMenu("Dosya");
        JMenu m2 = new JMenu("Yardım");
        mb.add(m1);
        mb.add(m2);
        // Dosya menüsüne alt menü ekledik.
        JMenuItem m11 = new JMenuItem("Aç");
        JMenuItem m22 = new JMenuItem("Farklı Kaydet");
        m1.add(m11);
        m1.add(m22);
        
        JPanel orta_panel = new JPanel(); // the panel is not visible in output
        //orta_panel.setBackground(Color.decode("#FFFF00"));
        // Orta kısımdaki textarea yı ekledik.
        

        JTextArea ta = new JTextArea();
        Dimension minimumSize = new Dimension(500, 200);
        ta.setMinimumSize(minimumSize);
        ta.setSize(500, 200);
        ta.setBackground(Color.pink);
        orta_panel.add(ta);
        
        
        // Nesneleri gruplamak için panel oluşturduk. Ve nesneleri panele ekledik.
        // Daha sonra bu paneli çerçeveye ekleyeceğiz.        
        JPanel alt_panel = new JPanel(); // the panel is not visible in output
        alt_panel.setBackground(Color.decode("#FFFF00"));
        //panel.setBackground(Color.green);
        
        JLabel label = new JLabel("Yazı giriniz : ");
        JTextField tf = new JTextField(10); // accepts upto 10 characters
        JButton send = new JButton("Gönder");
        JButton reset = new JButton("Temizle");
        
        alt_panel.add(label); // Bileşenler panele eklendi.
        alt_panel.add(tf);
        alt_panel.add(send);
        alt_panel.add(reset);

        
        // Tüm ana bileşenleri frame'e ekledik.
        
        frame.getContentPane().add(BorderLayout.NORTH, mb);
        frame.getContentPane().add(BorderLayout.CENTER, orta_panel);
        frame.getContentPane().add(BorderLayout.SOUTH, alt_panel);
        frame.setVisible(true);
        */
		
		
	}

}
